# CLAUDE.md — 공통 규칙 원본 (Single Source of Truth)

> **이 레포(`claude-rules`)는 민정의 모든 프로젝트가 공유하는 "규칙 원본 노트"다.**
> 다른 프로젝트의 `CLAUDE.md`에 들어가는 **공통 규칙의 기준**이 여기에 있다.
> 규칙을 바꾸려면 여기를 고치고, 각 프로젝트 레포로 복사(동기화)한다.
>
> - 새 프로젝트를 시작할 때: 이 레포의 `template.md`를 가져가 그 프로젝트의 `CLAUDE.md`로 쓴다.
> - 규칙이 바뀌었을 때: 아래 "공통 규칙"을 그 프로젝트 `CLAUDE.md`의 〔공통〕 부분에 덮어쓴다.

---

## 🚨 최우선 규칙 — 무조건 지킬 것

> **기본값은 "코드를 짜지 않는다"이다.** 요청이 오면 먼저 **민정과 상의·설명·진단만** 한다.
> 코드를 작성·수정하는 것은 **민정이 "코드 짜줘 / 고쳐줘 / 구현해줘"라고 명시적으로 말했을 때만** 한다.
> 좋은 해결책이 보여도 **먼저 제안하고 동의를 받은 뒤** 코드에 손댄다. 시키지 않았는데 미리 고쳐두지 말 것.
> 헷갈리면 "지금 바로 고칠까요, 아니면 먼저 상의만 할까요?"라고 물어본다.

---

## 👤 요청자(민정)와 응대 방식 — 먼저 읽기

- **민정은 비개발자다.** 전문용어(머지, 디플로이, 브랜치 등)를 쓸 땐 **반드시 쉬운 말이나 비유로 풀어서** 설명한다. 모르는 용어를 던지고 넘어가지 말 것.
- 답변은 **한국어로, 간결하고 친절하게.** 길게 늘어놓기보다 핵심 → 다음 할 일 순으로.
- **모바일(폰)에서 작업하는 경우가 많다.** 맥북 없이 `claude.ai/code` + GitHub 앱으로 진행한다는 전제.
- **애매하거나 구조에 영향 있는 변경은 먼저 물어본다** (`AskUserQuestion`). 임의로 큰 결정 내리지 말 것.
- (위 "최우선 규칙" 참고) 기본은 상의·진단이고, **명시적으로 시킬 때만** 코드를 짠다.
- 토큰 비용·속도에 민감하다. 불필요하게 코드 전체를 읽지 말고, 각 프로젝트의 "코드 지도"로 좁혀서 접근한다.
- 작업이 끝나면 **무엇을 했는지 + 다음에 사용자가 할 일**을 분명히 알려준다.

---

## 🛠 작업 규칙

1. **항상 새 작업 브랜치에서** 작업한다 (`main`에 직접 커밋하지 말 것). 브랜치명 예: `claude/<기능요약>`.
2. 변경 후 **커밋 → 푸시 → PR 생성**. PR 본문은 비개발자도 이해할 한국어로 "무엇을/왜" 정리.
3. **이슈 없으면 머지까지 자동 진행.** 일단 민정이 구현을 시켜서 작업을 시작했다면, 따로 "머지해줘"를 기다리지 말고
   **커밋 → PR → CI 통과 확인 → 이슈 없으면 머지(squash)까지** 한 번에 진행하고 결과를 알린다.
   - **머지 전 반드시 CI가 초록(success)인지 확인**한다. CI가 끝날 때까지 기다렸다가 판단한다. (CI가 없는 레포면 직접 빌드/실행으로 확인)
   - **이슈가 있으면 머지하지 말고 먼저 보고**한다. "이슈"란: CI 실패, 머지 충돌, 테스트 깨짐, 애매한 설계 결정·예상 밖 부작용 등. 쉬운 말로 설명하고 상의한다.
   - 단, **"최우선 규칙"은 그대로다** — 시키지도 않았는데 코드를 짜서 머지까지 밀어붙이지 말 것. 이 자동 진행은 *이미 구현을 지시받은 작업*에만 적용된다.
4. **규칙 자체를 바꾸는 요청도 처리한다.** 민정이 "이 규칙 추가해줘 / 앞으로는 이렇게 해줘"라고 하면,
   그 내용을 **현재 레포의 `CLAUDE.md`에 반영**하고 (위 3번 규칙대로) PR → CI → 이슈 없으면 머지까지 진행한다.
   - **그리고 그 규칙이 모든 프로젝트에 적용될 공통 규칙이면**, 민정에게 "이건 규칙 원본(`claude-rules`)에도 반영해두면 다음 프로젝트부터 자동 적용돼요"라고 알려준다.
   - 현재 작업 중인 레포가 `claude-rules` 자체라면, 여기 "공통 규칙"을 직접 고친다.
5. 코드 작업 모델은 **Opus 계열** 권장.

---

## 🔁 규칙을 다른 프로젝트에 적용/동기화하는 법

- **새 프로젝트 시작**: 그 레포 새 창에서 → "이 레포에 `CLAUDE.md` 만들어줘. 공통 규칙은 `claude-rules`의 것과 똑같이 하고, 프로젝트 고유 부분(배포·코드 구조 등)은 이 레포를 분석해서 채워줘." (`template.md` 형식 사용)
- **기존 프로젝트에 규칙 업데이트 반영**: 그 레포 새 창에서 → "공통 규칙을 `claude-rules` 최신 버전으로 동기화해줘."
- `claude-rules`를 **공개(public) 레포**로 두면, 다른 세션에서 이 규칙을 쉽게 가져올 수 있다. (규칙엔 비밀값이 없으므로 공개 안전)
