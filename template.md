# CLAUDE.md — 새 세션 인수인계 문서 (새 프로젝트용 템플릿)

> 새 프로젝트를 시작할 때 이 파일을 그 레포 루트에 `CLAUDE.md`로 복사해 쓴다.
> **「채워넣기」로 표시된 부분만** 그 프로젝트에 맞게 채우면 된다.
> (새 세션의 Claude에게 "이 레포 분석해서 「채워넣기」 부분 채워줘"라고 하면 코드를 보고 채운다.)
> 〔공통〕 부분은 `claude-rules`의 공통 규칙과 동일하게 유지한다.

---

## 🚨 최우선 규칙 — 무조건 지킬 것  〔공통〕

> **기본값은 "코드를 짜지 않는다"이다.** 요청이 오면 먼저 **민정과 상의·설명·진단만** 한다.
> 코드를 작성·수정하는 것은 **민정이 "코드 짜줘 / 고쳐줘 / 구현해줘"라고 명시적으로 말했을 때만** 한다.
> 좋은 해결책이 보여도 **먼저 제안하고 동의를 받은 뒤** 코드에 손댄다. 시키지 않았는데 미리 고쳐두지 말 것.
> 헷갈리면 "지금 바로 고칠까요, 아니면 먼저 상의만 할까요?"라고 물어본다.

---

## 👤 요청자(민정)와 응대 방식 — 먼저 읽기  〔공통〕

- **민정은 비개발자다.** 전문용어를 쓸 땐 **반드시 쉬운 말이나 비유로 풀어서** 설명한다.
- 답변은 **한국어로, 간결하고 친절하게.** 핵심 → 다음 할 일 순으로.
- **모바일(폰)에서 작업하는 경우가 많다.** 맥북 없이 `claude.ai/code` + GitHub 앱 전제.
- **애매하거나 구조에 영향 있는 변경은 먼저 물어본다** (`AskUserQuestion`).
- 기본은 상의·진단이고, **명시적으로 시킬 때만** 코드를 짠다.
- 토큰 비용·속도에 민감하다. 전체를 읽지 말고 아래 "코드 지도"로 좁혀서 접근한다.
- 작업이 끝나면 **무엇을 했는지 + 다음에 사용자가 할 일**을 분명히 알려준다.

---

## 🛠 작업 규칙  〔공통〕

1. **항상 새 작업 브랜치에서** 작업한다. 브랜치명 예: `claude/<기능요약>`.
2. 변경 후 **커밋 → 푸시 → PR 생성**. PR 본문은 비개발자도 이해할 한국어로.
3. **이슈 없으면 머지까지 자동 진행.** 구현을 지시받았으면 커밋 → PR → CI 통과 확인 → 이슈 없으면 머지(squash)까지 하고 결과를 알린다.
   - 머지 전 **CI 초록 확인** (CI 없으면 직접 빌드/실행으로 확인).
   - **이슈 있으면 머지 말고 먼저 보고** (CI 실패·충돌·애매한 결정 등).
   - "최우선 규칙"은 그대로 — 안 시키면 코드 짜서 밀어붙이지 말 것.
4. **규칙 변경 요청도 처리한다.** "규칙 추가해줘"라고 하면 이 `CLAUDE.md`에 반영하고, 공통 규칙이면 `claude-rules`에도 반영하라고 안내한다.
5. 코드 작업 모델은 **Opus 계열** 권장.

---

## 📦 이 프로젝트가 뭔지  〔채워넣기〕

〔채워넣기: 이 프로젝트가 무엇을 하는 것인지 1~3줄. 언어/스택도 함께〕

---

## 🚀 배포 구조  〔채워넣기 — 확인 필수〕

> 코드를 고치면 실제로 **어디에 어떻게 반영되는지**. (예: GitHub Pages / Vercel / Netlify / Railway 자동 배포 등)
> 레포의 배포 설정 파일(`.github/workflows/*`, `vercel.json`, `netlify.toml`, `package.json` scripts 등)을 보고 채운다. 모르면 민정에게 "이거 어디서 보세요?"라고 물어본다.

```
코드 수정 → 작업 브랜치 → PR → main에 머지 → 〔채워넣기: 배포 방식〕 → 실제 반영
```

---

## 🗺 코드 지도  〔채워넣기〕

| 파일/폴더 | 내용 |
|---|---|
| 〔채워넣기〕 | 〔채워넣기〕 |

---

## 🔑 설정 / 환경변수  〔채워넣기〕

> 비밀키·설정이 있으면 **어디 보관하는지**만 (값은 코드에 하드코딩 금지). 없으면 "없음".

---

## 🧪 테스트 / 확인 방법  〔채워넣기〕

> 변경 후 어떻게 확인하는지. (예: `npm run dev`로 띄워 보기, `npm run build` 통과 확인, 테스트 명령 등)

---

## ⚠️ 알아둘 함정 / 주의사항  〔일부 채워넣기〕

- **CI(자동 테스트)가 있는지 확인.** 없으면 직접 확인하고, 필요하면 추가를 제안한다.
- 〔채워넣기: 이 프로젝트에서 자주 깨지거나 조심할 부분〕

---

## 📁 파일 구조  〔채워넣기〕

| 파일 | 역할 |
|---|---|
| 〔채워넣기〕 | 〔채워넣기〕 |
